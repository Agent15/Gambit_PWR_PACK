# The Gambonanza build this checkout of the framework was last verified against.
#
# The framework binds to a lot of private game state by name (reflection), so a
# game update is the usual suspect when something stops working. build.sh hashes
# the vanilla Assembly-CSharp.dll on every run and prints a note when it differs
# from what's recorded here.
#
# To re-verify after a game update:
#   1. ./build.sh
#   2. Launch the game, open the console (F10), run `mods` and `list gambits`.
#   3. If everything works, update the two values below and note what changed.

steamBuildId=24142603
assemblySha256=eb14abe21cc2bd403404a4ab8612dc703d228d4da1a72bb47ebf64ac8f15488d
unityVersion=6000.4.3f1
verifiedOn=2026-07-26

# What this build changed that the framework cares about:
#   - SettingsCanvas gained a fifth "Customize" tab (m_Customize /
#     m_CustomizeContainer / m_Header_Customize) driving a new board colour-scheme
#     picker. Modal cloning now discovers tab containers/buttons by shape rather
#     than by name, so this and any future tab are stripped automatically.
#   - New types: SettingCustomizer, CustomizationManager, SchemeUnlockerManager,
#     TileSchemeColor; Library.TileSchemes[16]; BaseData.SchemeUnlocked;
#     SettingsData.TileIndex. Exposed via the `list schemes` / `scheme set` /
#     `scheme unlock` console commands.
#   - Everything else the framework reflects on (GameManager.Start,
#     CanvasMenu.OnEnable, SettingsCanvas.OnEnable, GambitLibrary buckets,
#     GambitCollectionSlide, RunInfoCanvas, StockManager, ChessDataManager,
#     TileBehaviour, BossManager) is unchanged. Vanilla gambit count is 200.
